const NAVBAR = "NAVBAR-TYPE"

let initialState = {}

//@ts-ignore
export const navbarReducer = (state = initialState, action) => {
    switch (action.type) {
        case NAVBAR :
            let newMessage = {

            }

            return state
        default:
            return state
    }
};

