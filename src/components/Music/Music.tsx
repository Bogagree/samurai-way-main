import React from "react";
import style from './Muisic.module.css'

type MusicPropsType = {}

export const Music = (props: MusicPropsType) => {
    return (
        <div>
            Music
        </div>
    )
}